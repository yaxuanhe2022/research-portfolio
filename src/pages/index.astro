---
const profile = {
  name: "Ya-Xuan He",
  email: "yaxuanhe@gapp.nthu.edu.tw",
  links: [
    { label: "CV", href: "/cv.pdf" },
    { label: "Personal website", href: "https://yaxuanhe.me/" },
    { label: "X", href: "https://x.com/yaxuanhe_zh" },
  ],
};

const projects = [
  {
    year: "2025-2026",
    title: "Detachable AI: Conceptualizing proactive interventions to reduce emotional over-reliance on AI",
    desc: "Human–AI Relationship · Conceptual Design",
    thumb: "/project-2.jpg",
    href: "/projects/detachable-ai",
  },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{profile.name}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
	<link
	href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;500;700&display=swap"
	rel="stylesheet"
	/>


    <style>
      :root {
        --bg: #0b0b0b;
        --text: #eaeaea;
        --muted: #a8a8a8;
        --rule: #262626;
        --card: #101010;
      }

      * { box-sizing: border-box; }

      body {
        margin: 0;
        background: var(--bg);
        color: var(--text);
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
        line-height: 1.6;
      }

      a { color: var(--text); text-decoration: none; }
      a:hover { text-decoration: underline; }

      .page {
        max-width: 900px;
        margin: 0 auto;
        padding: 40px 24px 70px;
      }

      .top {
        display: grid;
        grid-template-columns: 220px 1fr;
        gap: 28px;
        align-items: start;
      }

      .avatar {
        width: 180px;
        height: 180px;
        border-radius: 0px;
        overflow: hidden;
        background: #1b1b1b;
        margin-bottom: 16px;
      }

      .avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
      }

      .email {
        color: var(--muted);
        font-size: 14px;
        margin: 0 0 12px;
      }

      .links a {
        display: block;
        padding: 2px 0;
        font-size: 14px;
      }

      .title {
		font-family: "Libre Baskerville", serif;
		font-weight: 500;
		font-style: normal;
		font-size: 30px;
		margin: 0 0 12px;
		}


      .introBox {
		padding: 0;
		border: none;
		background: none;
		color: var(--muted);
		}


      .introBox p { margin: 0 0 12px; }
      .introBox p:last-child { margin: 0; }

      .divider {
        margin: 46px 0 26px;
        border-top: 1px solid var(--rule);
      }

      .sectionTitle {
		font-family: "Libre Baskerville", serif;
		font-weight: 500;
		font-size: 18px;
		margin: 0 0 20px;
		}


      .projectList {
        display: flex;
        flex-direction: column;
        gap: 22px;
      }

      .projectItem {
        display: grid;
        grid-template-columns: 260px 1fr;
        gap: 22px;
        align-items: center;
      }

      .thumb {
        width: 260px;
        height: 150px;
        border-radius: 0px;
        overflow: hidden;
        border: 1px solid var(--rule);
        background: #141414;
        display: block;
      }

      .thumb img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
      }

      .pTitle {
        display: inline-block;
        font-size: 18px;
        font-weight: 650;
        margin: 0 0 4px;
      }

      .pDesc {
        margin: 0;
        color: var(--muted);
        font-size: 14px;
      }

	  .pYear {
		font-size: 12px;
		color: var(--muted);
		letter-spacing: 0.08em;
		text-transform: uppercase;
		margin-bottom: 6px;
		}


      @media (max-width: 900px) {
        .top { grid-template-columns: 1fr; gap: 24px; }
        .title { font-size: 38px; }
        .projectItem { grid-template-columns: 1fr; }
        .thumb { width: 100%; height: 200px; }
      }
	  strong {
		color: #ffffff;
		font-weight: 600;
		}
    </style>
  </head>

  <body>
    <div class="page">
      <section class="top">
        <aside>
          <div class="avatar">
            <img src="/avatar.jpg" alt="Portrait" />
          </div>

          <p class="email">
            <a href={`mailto:${profile.email}`}>{profile.email}</a>
          </p>

          <div class="links">
            {profile.links.map((l) => (
              <a href={l.href} target={l.href.startsWith("http") ? "_blank" : undefined}>
                {l.label}
              </a>
            ))}
          </div>
        </aside>

        <div>
          <h1 class="title">{profile.name}</h1>

          <div class="introBox">
            <p>
              Hi there! I’m a master’s student in Statistics and Data Science at National Tsing Hua University (NTHU). Previously, I worked as a Data Specialist at LINE Taiwan and received my bachelor’s degree from National Taiwan University (NTU).
			</p>
			<p>
			  My research interests lie in Human–Computer Interaction (HCI), particularly in exploring how tools can <strong>broaden our imagination</strong> and <strong>shape human–AI relationships</strong>.
			</p>
            <p>
			  <strong>I am currently seeking research assistant or related research roles.</strong>
            </p>
          </div>
        </div>
      </section>

      <div class="divider"></div>

      <section>
        <h2 class="sectionTitle">Research</h2>

        <div class="projectList">
          {projects.map((p) => (
            <article class="projectItem">
              <a class="thumb" href={p.href} aria-label={`Open project: ${p.title}`}>
                <img src={p.thumb} alt={`Thumbnail for ${p.title}`} />
              </a>

              <div>
				<div class="pYear">{p.year}</div>
				<a class="pTitle" href={p.href}>{p.title}</a>
				<p class="pDesc">{p.desc}</p>
			  </div>

            </article>
          ))}
        </div>
      </section>
    </div>
  </body>
</html>
