---
const project = {
  title: "Detachable AI",
  subtitle:
    "A research proposal on proactive interventions for healthier human–AI relationships",
  year: "2025–2026",
  status: "Conceptual Design",
  hero: "/project-2.jpg",
  links: [
    // 你可以留空或之後補上
    // { label: "Slides (PDF)", href: "/detachable-ai-slides.pdf", external: false },
    // { label: "Repo", href: "https://github.com/...", external: true },
  ],
  overview: [
    "Detachable AI explores psychological theory–based proactive interventions designed to help people disengage from AI chatbots when interactions evolve into emotional over-reliance.",
  ],
  motivation: [
    "AI chatbots are increasingly used for emotional support and companionship. Prior work shows that users can develop emotional dependence over time, and recent studies have begun to measure problematic reliance. However, most research remains descriptive. Few systems proactively intervene when emotional over-reliance emerges, highlighting an opportunity for psychologically grounded design interventions that support healthier disengagement.",
    "To address this gap, we explore three psychologically grounded intervention mechanisms, leading to the following research questions."
  ],
  rqs: [
    {
      q: "RQ1 · Awareness",
      text:
        "How do awareness-focused interventions support users in maintaining awareness of their AI usage patterns?",
    },
    {
      q: "RQ2 · Reflection",
      text:
        "How does summarizing users’ conversations influence their self-reflection and their perceived relationship with AI?",
    },
    {
      q: "RQ3 · Redirection",
      text:
        "How can conversational strategies gently encourage users to shift attention toward real-world social connections?",
    },
  ],
  design: [
    "Study format: 4-week deployment with weekly check-ins and post-study interview.",
    "Design: between-subject conditions comparing baseline vs. intervention variants.",
    "Data: behavioral logs (usage frequency, session duration), weekly survey measures, and qualitative interviews.",
  ],
  conditions: [
    {
        title: "Awareness",
        bullets: [
        "Visualize usage patterns (e.g., time spent) to increase users’ awareness of their interaction behavior.",
        "Support users in noticing when usage intensifies.",
        ],
    },
    {
        title: "Reflection",
        bullets: [
        "Provide conversation summaries to help users step back from in-the-moment emotions.",
        "Encourage reflection on recurring emotional themes and reliance patterns.",
        ],
    },
    {
        title: "Redirection",
        bullets: [
        "Use gentle prompts that recall meaningful people or experiences.",
        "Encourage reconnection with real-world social ties without being judgmental or abrupt.",
        ],
    },
    ],
  analysis: [
    "Quantitative: compare weekly survey changes across conditions (e.g., dependency, self-reflection/insight, perceived relationship quality).",
    "Behavioral: analyze shifts in usage patterns over time (e.g., sessions, frequency, length).",
    "Qualitative: thematic analysis of interviews to understand perceived helpfulness, friction, and acceptance of interventions.",
  ],
  reflection: [
    "Structured and synthesized relevant literature into a research framing.",
    "Articulated theory-driven research questions.",
    "Prototyped conceptual interventions to support design communication.",
  ],
};
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{project.title} · Ya-Xuan He</title>

    <!-- match your site heading font -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;500;700&display=swap"
      rel="stylesheet"
    />

    <style>
      :root {
        --bg: #0b0b0b;
        --text: #eaeaea;
        --muted: #a8a8a8;
        --rule: #262626;
        --card: #101010;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        background: var(--bg);
        color: var(--text);
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
        line-height: 1.65;
      }
      a { color: var(--text); text-decoration: none; }
      a:hover { text-decoration: underline; }

      .page {
        max-width: 980px;
        margin: 0 auto;
        padding: 48px 24px 80px;
      }

      .topnav {
        margin-bottom: 22px;
        display: flex;
        align-items: center;
        gap: 12px;
        color: var(--muted);
        font-size: 14px;
      }

      .topnav a { color: var(--muted); }
      .dot { opacity: 0.6; }

      .header {
        display: grid;
        grid-template-columns: 280px 1fr;
        gap: 28px;
        align-items: start;
      }

      .hero {
        width: 280px;
        height: 168px;
        border-radius: 12px;
        overflow: hidden;
        border: 1px solid var(--rule);
        background: #141414;
      }
      .hero img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
      }

      .hTitle {
        font-family: "Libre Baskerville", serif;
        font-weight: 500;
        font-size: 40px;
        margin: 0 0 10px;
        letter-spacing: -0.01em;
      }

      .sub {
        color: var(--muted);
        margin: 0 0 10px;
        max-width: 720px;
      }

      .metaRow {
        display: flex;
        flex-wrap: wrap;
        gap: 10px 16px;
        margin-top: 12px;
        color: var(--muted);
        font-size: 13px;
      }

      .pill {
        border: 1px solid var(--rule);
        background: var(--card);
        padding: 6px 10px;
        border-radius: 999px;
      }

      .links {
        margin-top: 14px;
        display: flex;
        gap: 14px;
        flex-wrap: wrap;
      }

      .links a {
        font-size: 13px;
        color: var(--text);
        border-bottom: 1px solid transparent;
      }

      .divider {
        margin: 34px 0 0;
        border-top: 1px solid var(--rule);
      }

      .content {
        margin-top: 30px;
        display: grid;
        grid-template-columns: 1fr;
        gap: 26px;
      }

      .section {
        padding-top: 10px;
      }

      .section h2 {
        font-family: "Libre Baskerville", serif;
        font-weight: 500;
        font-size: 20px;
        margin: 0 0 12px;
      }

      .section p {
        margin: 0 0 12px;
        color: var(--muted);
      }

      .section p strong {
        color: var(--text);
        font-weight: 650;
      }

      .grid2 {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 18px;
      }

      .card {
        border: 1px solid var(--rule);
        background: var(--card);
        border-radius: 12px;
        padding: 16px 16px;
      }

      .card h3 {
        margin: 0 0 10px;
        font-size: 14px;
        font-weight: 650;
        color: var(--text);
      }

      ul {
        margin: 0;
        padding-left: 18px;
        color: var(--muted);
      }

      li { margin: 6px 0; }

      .rqList {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }

      .rqItem {
        border: 1px solid var(--rule);
        background: var(--card);
        border-radius: 12px;
        padding: 14px 16px;
      }

      .rqItem .tag {
        font-size: 12px;
        color: var(--muted);
        letter-spacing: 0.08em;
        text-transform: uppercase;
        margin-bottom: 6px;
      }

      .rqItem .rqText {
        color: var(--text);
        margin: 0;
      }

      @media (max-width: 900px) {
        .header { grid-template-columns: 1fr; }
        .hero { width: 100%; height: 220px; }
        .hTitle { font-size: 34px; }
        .grid2 { grid-template-columns: 1fr; }
      }

      .conditionImageSingle {
        margin: 20px 0 28px;
        }

        .conditionImageSingle img {
        width: 100%;
        max-width: 820px;
        border-radius: 16px;
        border: 1px solid var(--rule);
        display: block;
        }

        .conditionItem {
        margin-bottom: 26px;
        }

        .conditionTextBlock h3 {
        font-family: "Libre Baskerville", serif;
        font-weight: 500;
        font-size: 14px; /* title smaller */
        letter-spacing: 0.06em;
        text-transform: uppercase;
        margin: 0 0 8px;
        color: var(--text);
        }

        .conditionTextBlock ul {
        margin: 0;
        padding-left: 18px;
        color: var(--muted);
        }

        .conditionTextBlock li {
        margin: 6px 0;
        }
}

    </style>
  </head>

  <body>
    <div class="page">
      <div class="topnav">
        <a href="/">Home</a>
        <span class="dot">/</span>
        <span>{project.title}</span>
      </div>

      <header class="header">
        <div class="hero">
          <img src={project.hero} alt={`${project.title} thumbnail`} />
        </div>

        <div>
          <h1 class="hTitle">{project.title}</h1>
          <p class="sub">{project.subtitle}</p>

          <div class="metaRow">
            <span class="pill">{project.year}</span>
            <span class="pill">{project.status}</span>
          </div>

          {project.links.length > 0 && (
            <div class="links">
              {project.links.map((l) => (
                <a href={l.href} target={l.external ? "_blank" : undefined}>
                  {l.label}
                </a>
              ))}
            </div>
          )}
        </div>
      </header>

      <div class="divider"></div>

      <main class="content">
        <section class="section" id="overview">
          <h2>Overview</h2>
          {project.overview.map((t) => (
            <p>{t}</p>
          ))}
        </section>

        <section class="section" id="motivation">
          <h2>Motivation</h2>
          {project.motivation.map((t) => (
            <p>{t}</p>
          ))}
        </section>

        <section class="section" id="rqs">
          <h2>Research Questions</h2>
          <div class="rqList">
            {project.rqs.map((r) => (
              <div class="rqItem">
                <div class="tag">{r.q}</div>
                <p class="rqText">{r.text}</p>
              </div>
            ))}
          </div>
        </section>

        <section class="section" id="design">
          <h2>Experimental Design</h2>
          <div class="card">
            <ul>
              {project.design.map((t) => (
                <li>{t}</li>
              ))}
            </ul>
          </div>
        </section>

        <section class="section" id="conditions">
            <h2>Intervention Conditions</h2>

            <div class="conditionImageSingle">
                <img
                src="/conditions-overview.png"
                alt="Overview of Awareness, Reflection, and Redirection interventions"
                />
            </div>

            <div class="conditionTextBlock">
                {project.conditions.map((c) => (
                <div class="conditionItem">
                    <h3>{c.title}</h3>
                    <ul>
                    {c.bullets.map((b) => (
                        <li>{b}</li>
                    ))}
                    </ul>
                </div>
                ))}
            </div>
        </section>

        <section class="section" id="analysis">
          <h2>Planned Analysis</h2>
          <div class="card">
            <ul>
              {project.analysis.map((t) => (
                <li>{t}</li>
              ))}
            </ul>
          </div>
        </section>

        <section class="section" id="reflection">
            <h2>Reflection & Lessons Learned</h2>

            <div class="card">
                <ul class="reflectionList">
                {project.reflection.map((t) => (
                    <li>{t}</li>
                ))}
                </ul>
            </div>
        </section>

      </main>
    </div>
  </body>
</html>
